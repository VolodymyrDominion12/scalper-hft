[project]
name = "scalper-hft"
version = "0.1.0"
description = "Високочастотна скальпінг-система для Binance USDT-M ф'ючерсів: дані, фічі, стратегії, бектест з анти-перенавчанням, ML, paper/live торгівля."
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.26",
    "pandas>=2.1",
    "pyarrow>=15.0",
    "python-dotenv>=1.0",
    "requests>=2.31",
    "ccxt>=4.2",
    "scipy>=1.11",
    "statsmodels>=0.14",
    "matplotlib>=3.8",
    "plotly>=5.20",
    "tabulate>=0.9",
    "psycopg[binary]>=3.2",
    "pyyaml>=6.0",
    "ipykernel>=7.3.0",
    "pandas-stubs~=3.0.5",
]

[project.optional-dependencies]
optim = [
    "optuna>=3.6",
]
ml = [
    "scikit-learn>=1.4",
    "lightgbm>=4.3",
]
dashboard = [
    "streamlit>=1.30",
    "bcrypt>=4.0",
]
api = [
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
    "pyjwt>=2.8.0",
]
live = [
    "websockets>=12",
    "python-telegram-bot>=20.8",
]
hft = [
    "nautilus_trader>=1.201.0",
]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.10",
    "pandas-stubs>=2.1",
    "types-requests>=2.31",
    "httpx>=0.27.0",
]
notebook = [
    "ipywidgets>=8.1.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["scalper_hft"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 120
target-version = "py312"
src = ["scalper_hft", "tests", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"*.ipynb" = ["E402"]
"scalper_hft/dashboard.py" = ["E402"]

[tool.mypy]
python_version = "3.12"
strict = false
files = ["scalper_hft"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = [
    "scalper_hft.live.money",
    "scalper_hft.live.account",
    "scalper_hft.live.pending_orders",
    "scalper_hft.live.sync_engine",
    "scalper_hft.live.pairs_engine",
    "scalper_hft.live.trader_bars",
    "scalper_hft.live.trader_loop",
    "scalper_hft.live.store",
]
strict = true

[tool.uv]
package = true
